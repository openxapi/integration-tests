# Binance Options REST API Integration Test Configuration
# Copy this file to env.local and fill in your credentials
# Use testnet credentials for safety: https://testnet.binance.vision/

# HMAC Authentication (default)
export BINANCE_API_KEY=""
export BINANCE_SECRET_KEY=""

# RSA Authentication (optional)
export BINANCE_RSA_API_KEY=""
export BINANCE_RSA_PRIVATE_KEY_PATH="/path/to/rsa_private_key.pem"

# Ed25519 Authentication (optional)
export BINANCE_ED25519_API_KEY=""
export BINANCE_ED25519_PRIVATE_KEY_PATH="/path/to/ed25519_private_key.pem"

# Server Configuration (defaults to production - NO TESTNET AVAILABLE)
# export BINANCE_OPTIONS_REST_SERVER="https://eapi.binance.com"  # Production (default)
# WARNING: Binance Options API does not have a dedicated testnet environment

# =============================================================================
# TEST FEATURE TOGGLES
# =============================================================================
# Set to "true" to enable specific test operations
# WARNING: These operations may involve real money/assets - use with caution!

# Options Trading
export BINANCE_TEST_OPTIONS_TRADING="false"        # Enable options trading tests
export BINANCE_TEST_OPTIONS_BATCH_ORDERS="false"   # Enable batch order tests
export BINANCE_TEST_OPTIONS_CANCEL_ALL="false"     # Enable cancel all orders tests

# Block Trading
export BINANCE_TEST_BLOCK_TRADING="false"          # Enable block trading tests
export BINANCE_TEST_BLOCK_ORDERS="false"           # Enable block order tests

# Market Maker Protection
export BINANCE_TEST_MMP_OPERATIONS="false"         # Enable MMP operations

# Kill Switch
export BINANCE_TEST_KILL_SWITCH="false"            # Enable kill switch tests

# User Data Stream
export BINANCE_TEST_USER_DATA_STREAM="true"        # Enable user data stream tests

# Account Operations
export BINANCE_TEST_ACCOUNT_OPERATIONS="true"      # Enable account info tests
export BINANCE_TEST_POSITION_OPERATIONS="true"     # Enable position info tests
export BINANCE_TEST_TRADING_HISTORY="true"         # Enable trading history tests

# Market Data
export BINANCE_TEST_MARKET_DATA="true"             # Enable market data tests
export BINANCE_TEST_HISTORICAL_DATA="true"         # Enable historical data tests

# =============================================================================
# ACCOUNT INFORMATION
# =============================================================================
# Required for options-specific tests (optional)

# Options Account Testing
export BINANCE_OPTIONS_ACCOUNT_EMAIL=""            # Options account email for testing
export BINANCE_OPTIONS_UNDERLYING_ASSET="BTC"      # Default underlying asset for testing

# =============================================================================
# TEST ORDER IDS
# =============================================================================
# Optional - for testing specific order operations
# These should be valid order IDs from your options account

export BINANCE_TEST_OPTIONS_ORDER_ID=""            # Valid options order ID
export BINANCE_TEST_OPTIONS_SYMBOL=""              # Valid options symbol (e.g., BTC-240329-50000-C)
export BINANCE_TEST_BLOCK_ORDER_ID=""              # Valid block order ID

# =============================================================================
# ADDITIONAL AUTHENTICATION OPTIONS
# =============================================================================
# Set to "true" to test all authentication methods (HMAC, RSA, Ed25519)
# Default: only Ed25519 is tested to save time
export TEST_ALL_AUTH_TYPES="false"

# =============================================================================
# OPTIONS SPECIFIC SETTINGS
# =============================================================================
# Options trading specific configuration

# Default test values for options trading
export BINANCE_TEST_OPTIONS_QUANTITY="0.01"        # Default quantity for test orders
export BINANCE_TEST_OPTIONS_PRICE="100"            # Default price for test orders
export BINANCE_TEST_OPTIONS_SIDE="BUY"             # Default side for test orders
export BINANCE_TEST_OPTIONS_TYPE="LIMIT"           # Default order type for test orders

# Risk management for options tests
export BINANCE_TEST_MAX_ORDER_VALUE="10"           # Maximum order value for safety
export BINANCE_TEST_OPTIONS_TIMEOUT="30"           # Timeout for options operations (seconds)