# Binance API Credentials for Options WebSocket Integration Tests
# IMPORTANT: Options WebSocket API only supports mainnet - no testnet available
# Use real credentials but with minimal permissions (read-only recommended)

# Your Binance API Key
BINANCE_API_KEY=your_api_key_here

# Your Binance Secret Key  
BINANCE_SECRET_KEY=your_secret_key_here

# Test verbosity (optional)
TEST_VERBOSE=false

# Usage:
# 1. Copy this file to env.local
# 2. Fill in your actual API credentials
# 3. Source the file: source env.local
# 4. Run tests: go test -v

# Security Notes:
# - Never commit env.local to version control
# - Use API keys with minimal required permissions
# - Consider using read-only API keys for testing
# - Options WebSocket requires authenticated access for all operations